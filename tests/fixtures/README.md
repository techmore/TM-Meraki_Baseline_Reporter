# Test Fixtures

These JSON and Markdown files are sanitized inputs used by the automated test
suite. They should stay deterministic so report and backup behavior can be
tested without live Meraki API access.

Do not copy real `backups/<org>/` data into this directory unless it has been
fully sanitized.
